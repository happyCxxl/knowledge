# knowledge 实施手册

> 本文档是项目的统一实施基准：模块架构、总体实施顺序、每阶段的环节操作流程、提交与验证约定。
> 每一步做什么、按什么流程做、怎么验收，以本文档为准；
> 每个功能的历史设计档案（决策与实现口径）见 `docs/steps/`、`docs/implementations/`、`docs/handover/`。

## 1. 项目定位

- **单体应用**：后端单进程；前端（knowledge-web）后续配套建设；
- **登录/鉴权自建**：用户、令牌、安全配置均在本项目内实现（不做平台共享能力假设）；
- **无租户概念**：数据模型不含 tenant 语义；
- **不调用外部业务服务**：文件存储、向量库等均为项目自管组件；外部能力仅限开源基础设施（MySQL/Redis/Milvus）；
- **按模块拆分，而非分项目**：模块间为工程分层关系，同一构建与部署单元。

## 2. 模块清单与依赖方向

```
knowledge-infra          （零依赖：数据与规范层）
    ↑
knowledge-common         （依赖 infra：领域模型、DTO、枚举、错误码、R 契约、用户模型）
    ↑
knowledge-auth           （依赖 common：登录、用户、令牌、安全配置）
knowledge-storage        （依赖 common：文件 + 产物对象存储）
knowledge-vector         （依赖 infra：Milvus 向量库封装）
    ↑
knowledge-worker         （依赖 common + storage + vector：文档处理引擎）
knowledge-biz            （依赖全部：业务接口层）
knowledge-api            （依赖 common：对外契约，暂为空壳）
```

| 模块                | 职责                                                                                                                        | 成立时机  |
|---------------------|-----------------------------------------------------------------------------------------------------------------------------|-----------|
| `knowledge-infra`   | 数据与规范层：五件套基类（BaseInfo/BaseCreateInfo）、雪花主键与 VO Long→String 约定、DbService 模式、Redis 队列组件、通用锁 | 阶段 1    |
| `knowledge-common`  | 领域模型、实体、枚举、错误码、异常、统一返回契约 R、用户模型（KnowledgeUser）与安全上下文工具（SecurityUtils）              | 阶段 2    |
| `knowledge-auth`    | 认证与用户：登录/注册、用户表、JWT、Spring Security 过滤链                                                                  | 阶段 2    |
| `knowledge-storage` | 对象存储：文件（fileId 寻址）+ 产物（sha256 内容寻址）两套端口、本地磁盘实现                                                | 阶段 4    |
| `knowledge-vector`  | 向量库封装：Milvus 客户端生命周期、通用集合管理、查询封装、版本/口径钉版                                                    | 阶段 16   |
| `knowledge-worker`  | 文档处理引擎：输入校验、解析、组装、预处理、切片、向量化、索引                                                              | 阶段 5 起 |
| `knowledge-biz`     | 业务接口层：控制器、服务编排、任务消费、知识库管理、检索评测                                                                | 阶段 3 起 |
| `knowledge-api`     | 对外契约模块（对外 DTO/接口，前端接入阶段启用）                                                                             | 阶段 19   |

## 3. 总体实施顺序

| 阶段 | 名称                           | 工作内容                                                                                                     | 本阶段自实现            | 参考档案     |
|------|--------------------------------|--------------------------------------------------------------------------------------------------------------|-------------------------|--------------|
| 0    | 工程基线与数据库落地           | 四模块骨架、迁移脚本体系、Docker 编排、文档骨架                                                              | —                       | step-00      |
| 1    | 基础设施模块 knowledge-infra   | 新建模块：五件套基类、通用约定与工具落位                                                                     | —                       | step-00/01   |
| 2    | 通用契约与用户认证             | common：R、ErrorCode、异常、KnowledgeUser、SecurityUtils；auth：用户表、登录/注册、JWT、Spring Security 配置 | R、认证全套             | —（自建）    |
| 3    | 核心领域模型与知识库管理闭环   | 知识库实体/枚举/DTO（common）；管理闭环接口 + 操作审计（真实用户落账）                                       | —                       | step-01      |
| 4    | 对象存储模块 knowledge-storage | 新建模块：文件端口 + 产物端口 + 本地实现（统一对象存储机制）                                                 | 本地文件/产物存储实现   | 架构设计 A–J |
| 5    | B01 文档输入                   | 提交链、流式校验、建档、任务登记、查询接口                                                                   | —（复用 storage）       | step-02      |
| 6    | 任务衔接与 B01 改造            | Redis 队列、幂等冲突、流式校验改造、格式白名单                                                               | Redis 队列组件（infra） | step-03      |
| 7    | B02 文档解析                   | 消费循环、原生解析（PDF/Office）、六信号、产物落库                                                           | —                       | step-04      |
| 8    | B03 统一文档模型               | STRUCTURE 环节：标准化、阅读顺序、结构组装、溯源                                                             | —                       | step-05      |
| 9    | B04 数据预处理                 | PREPROCESS 环节 + 策略版本表                                                                                 | —                       | step-06      |
| 10   | B05 切片策略                   | CHUNK 环节：四路切片、父子层级、溯源                                                                         | —                       | step-07      |
| 11   | 切片策略扩展                   | 算法菜单化：正文/表格/兜底多算法 + 参数化                                                                    | —                       | step-08      |
| 12   | 策略管理菜单与知识库绑定       | 策略 CRUD + 知识库-策略绑定                                                                                  | —                       | step-09      |
| 13   | 预处理策略参数化与绑定         | 规则分组参数化 + 绑定扩展                                                                                    | —                       | step-10      |
| 14   | B07 向量化                     | EMBED 环节：前置校验、账本复用、四关、落盘                                                                   | 本地向量化实现          | step-11      |
| 15   | 执行链工作台与发布             | 执行树、产物内容、策略集合批量绑定                                                                           | —                       | step-12      |
| 16   | 向量库模块 knowledge-vector    | 新建模块：Milvus 封装（客户端/集合/查询/口径），业务层改为调其接口                                           | Milvus 封装下沉         | step-13      |
| 17   | 索引构建与发布                 | 组合模型、追加路径、对账、发布/回退                                                                          | —（复用 vector）        | step-13      |
| 18   | 检索与评测                     | B09 检索（双通道 + RRF）+ B10 评测测试台                                                                     | —                       | step-14      |
| 19   | 前端（预留）                   | knowledge-web；knowledge-api 契约启用                                                                        | —                       | —            |

## 4. 每阶段统一操作流程（八步）

> 全部阶段（含新建模块的 1/2/4/16）按本流程执行；流程源自 `docs/环节整理与交接规范.md` 的七步工作流，扩展了 SQL 迁移一步。

```
① 通读参考档案    该阶段的 step 任务文档（目标/决策/任务清单/验收）+ implementations 实现文档
                   （类/字段/接口口径）+ handover 交接文档（代码地图/表依赖）——先理解"做什么、怎么做、验收标准"
② 输出实施清单    助手给出精确清单：模块落位 + 代码文件（含 test）+ 迁移脚本 + 本阶段自实现类的设计要点
③ 逐项确认        用户审阅清单与设计，拍板后开工；有分歧先讨论再动手
④ 执行修改        代码/模块/迁移脚本按清单落地；测试同步（装配变化同步单测，纯映射类用真实实例）
⑤ SQL 迁移        执行本阶段 stage-XX-*.sql 并验证表结构与 seed 落库（表随阶段出现，不提前建全）
⑥ 验证            mvn compile：本阶段清单内文件零错误（后续阶段文件的编译错误属预期，忽略）；
                   能跑的单测即时跑；跑不了的登记到"全量测试"清单（契约补齐后统一执行）
⑦ 暂存与提交      助手 git add 清单 → 用户自查核实 → 用户提交（Conventional Commits）
⑧ 文档随环节提交   本阶段 step 档案 / implementations 实现文档 / handover 交接文档与代码同批提交
```

## 5. 自实现清单（遇到才做）

| 阶段 | 自实现项                        | 说明                                                      |
|------|---------------------------------|-----------------------------------------------------------|
| 2    | R、KnowledgeUser、SecurityUtils | 统一响应契约 + 用户模型与安全上下文（认证模块同阶段落地） |
| 2    | 认证全套                        | 用户表、登录/注册、JWT、Spring Security 过滤链            |
| 4    | 文件/产物存储                   | 本地磁盘实现（原文件服务契约重写为本地端口）              |
| 6    | Redis 队列组件                  | 放 infra（LPUSH/BRPOP 封装 + 分布式锁 + 配置类）          |
| 14   | 本地向量化                      | 替代外部网关（直连模型或本地实现，方案届时定）            |
| 16   | Milvus 封装                     | 通用集合管理/查询/生命周期，SDK 版本钉版                  |

## 6. 提交与验证约定

- **提交由用户执行**；助手负责清单、实现协助与验证，不执行 commit/push；
- **注释规范**：类/方法注释只写"做什么"（职责与行为），不写给谁用、为什么存在、从哪来（大厂规范）；
- 提交信息：Conventional Commits，如 `feat(step-03): ...`、`chore(infra): ...`；
- **SQL 按环节迁移**：迁移脚本以阶段编号命名（`stage-XX-*.sql`），每阶段提交时执行对应脚本（表随阶段出现）；Docker 自动初始化仅对全新环境生效；
- **验收口径**：`mvn compile` 本阶段文件零错误；全部契约补齐后统一跑 `mvn -Prun-tests test`；
- 每阶段结束记录该阶段执行结果（可在对应 step 档案追加执行记录）。

## 7. 交接文档与阶段映射

| 交接文档        | 对应阶段 |
|-----------------|----------|
| 01 文档输入     | 5        |
| 02 文档解析     | 7        |
| 03 统一文档模型 | 8        |
| 04 数据预处理   | 9        |
| 05 切片策略     | 10       |
| 06 向量化       | 14       |
