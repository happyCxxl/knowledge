# knowledge 项目文档说明

> 统一实施基准（模块架构 + 实施顺序 + 环节操作流程 + 提交与验证约定）见 `实施手册.md`，本文档只做目录说明。

## 目录组织

| 目录/文件     | 放什么                                                                                  |
|---------------|-----------------------------------------------------------------------------------------|
| `实施手册.md` | 项目唯一实施基准：模块架构、阶段顺序、每阶段八步操作流程、提交与验证约定                |
| `steps`      | 每阶段一份实施文档（`step-XX-环节名.md`）：目标/决策/任务清单/验收/执行记录，随阶段新增 |
| `../../README.md`   | 本说明                                                                                  |

## 参考档案

原项目的设计档案（step 任务文档 / implementations 实现文档 / handover 交接文档）保留在 git stash 与 `E:\workbuddy\knowledge-work` 原目录，仅作实施参考，不在本仓库维护。

## 提交约定

- 代码/文档的 git 提交一律由用户本人执行，助手不执行 `git commit` / `git push`；
- 助手完成改动后只负责 `git add` 暂存、汇报改动清单与验证结果。

## 关联文件

- SQL 迁移脚本（按阶段执行）：`../knowledge-biz/src/main/resources/sql/stage-XX-*.sql`，表随阶段出现，执行时以各脚本内 `USE` 语句为准。
