# 阶段 0：工程基线与数据库落地（实施文档）

> 约定：每个实施阶段在 `` 落一份实施文档，记录目标、决策、任务清单、验收标准与执行结果。

## 元信息

| 项       | 内容                                                     |
|----------|----------------------------------------------------------|
| 阶段     | 0（工程基线与数据库落地）                                |
| 状态     | 已完成（已提交：`chore: 项目初始框架` + `工程基线配套`） |
| 方案依据 | 自建（无参考档案）                                       |

## 1. 目标与范围

**目标**：搭起可运行的多模块工程骨架与本地开发环境，为后续阶段提供统一基底。

1. 多模块骨架：root pom + `knowledge-api`/`knowledge-biz`/`knowledge-common`/`knowledge-worker` 四模块占位、`KnowledgeApplication` 应用入口、`application.yml` 本地配置（Nacos 关闭）；
2. 建库迁移脚本体系：`stage-00-建库.sql`（knowledge 库，自包含 `USE` 语句）；
3. Docker 编排：MySQL 8 + Redis 7 + Milvus standalone（含 etcd/minio 依赖容器）；
4. 文档骨架：`../README.md` + `实施手册.md`（阶段 1 落位）；
5. `../../../.gitignore` 基线（含 `/common/`、`/logs/`、`/data/` 等锚定修正）。

## 2. 本阶段决策（记录）

| 编号 | 决策                                                                                                          | 依据                 |
|------|---------------------------------------------------------------------------------------------------------------|----------------------|
| D0-1 | JDK 统一 21；Spring Boot 3.2.5 parent；Spring Cloud 2023.0.1 + Alibaba 2023.0.1.0（Nacos 依赖保留、配置关闭） | 用户拍板             |
| D0-2 | SQL 按阶段拆分迁移脚本（`stage-XX-*.sql`），每阶段执行对应脚本，表随阶段出现                                  | 用户拍板（方案 B）   |
| D0-3 | git 提交一律由用户执行，助手只负责 `git add` 暂存                                                             | 用户拍板             |
| D0-4 | 注释规范：类/方法注释只写"做什么"（职责与行为）                                                               | 用户拍板（大厂规范） |
| D0-5 | 建库脚本自包含 `USE knowledge;`（Docker 初始化每个脚本独立会话）                                              | 实测修正             |

## 3. 任务清单

1. 多模块骨架与运行配置（9 文件：root pom、四模块 pom、应用入口、application.yml、.gitignore、根 README）；
2. Docker 编排（compose + docker/README）；
3. 建库脚本 `stage-00-建库.sql`；
4. 文档骨架（docs/README、实施手册）。

## 4. 验收标准

- [x] `mvn compile` 通过
- [x] MySQL 容器可初始化 knowledge 库
- [x] 各基础设施容器健康

## 5. 执行记录

| 任务                              | 执行结果 | 备注                             |
|-----------------------------------|----------|----------------------------------|
| 多模块骨架                        | 完成     | 提交 `c58fecb`（9 文件 +719 行） |
| Docker 编排 + 建库脚本 + 文档骨架 | 完成     | 提交 `9790a3f`（11 文件）        |

## 6. 下一步预告

阶段 1：基础设施模块 knowledge-infra。
