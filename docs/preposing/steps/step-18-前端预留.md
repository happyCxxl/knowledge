# 阶段 18：前端（预留）（实施文档）

> 约定：每个实施阶段在 `` 落一份实施文档，记录目标、决策、任务清单、验收标准与执行结果。

## 元信息

| 项       | 内容                                                   |
|----------|--------------------------------------------------------|
| 阶段     | 18（前端预留：knowledge-web 后续配套建设）             |
| 状态     | 已完成（待提交）                                       |
| 方案依据 | 实施手册 §3 阶段 18 + 单体应用口径（前端后续配套建设） |

## 1. 目标与范围

**目标**：本阶段为预留阶段，不建设前端代码——项目口径为**后端单进程单体**（实施手册 §1），前端 knowledge-web 随前端接入轮次建设。本阶段只落一份实施文档，记录预留边界与后续启用路径。

1. **不做**：knowledge-web 前端工程（页面/路由/构建）；
2. **不做**：对外契约模块（阶段 19 决策取消，能力一律在当前项目内实现）；
3. **预留边界**：
   - 前端检索/评测页面（检索入口、测试台双轴对比、规则配置页）随前端轮次建设；
   - 后端 API 面已就绪（阶段 14 工作台接口 + 阶段 16 索引控制面 + 阶段 17 检索与评测接口），前端接入时零后端改造；
4. **文档**：本档案。

## 2. 本阶段决策（记录）

| 编号  | 决策                                                                          | 依据             |
|-------|-------------------------------------------------------------------------------|------------------|
| D18-1 | 前端预留不建工程；后端 API 面（工作台/索引/检索评测）作为前端接入契约先行沉淀 | 实施手册单体口径 |
| D18-2 | 对外契约按用户决策取消（阶段 19 移除 knowledge-api 模块与预留契约）           | 用户决策         |

## 3. 任务清单

1. 文档：本档案。

## 4. 验收标准

- [x] 无代码变更（纯预留阶段）
- [x] `mvn test` 全绿（555 例，与阶段 17 同）
- [x] 文档随环节提交

## 5. 执行记录

| 任务   | 执行结果 | 备注                                  |
|--------|----------|---------------------------------------|
| 1 文档 | 完成     | 预留边界与启用路径记录；无代码/无 SQL |
